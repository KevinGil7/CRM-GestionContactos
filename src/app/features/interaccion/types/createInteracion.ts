export interface createInteracion{
    contactoId ?: string;
    tipo : string;
    asunto : string;
    notas : string;
}

export interface updateInteracion{
    tipo : string;
    asunto : string;
    notas : string;
}